[build]
  publish = "."

# Pretty URLs for blog posts (e.g. /blog/my-article -> /blog/my-article.html)
[[redirects]]
  from = "/blog/:slug"
  to = "/blog/:slug.html"
  status = 200
  conditions = {Role = [""]}
  force = false
